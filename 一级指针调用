#include<stdio.h>
void print(int *str,int sz)
{
	for(int i=0;i<sz;i++)
	{
		printf("%d",*(str+i));
	}
	
	
	
}
int main()
{
	int arr[10]={1,2,3,4,5,6,7,8,9};
	int *p=arr;
	int sz=sizeof(arr)/sizeof(arr[0]);
	print(p,sz);	
	return 0;
}
