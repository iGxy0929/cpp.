#include<stdio.h>
void Init(int a[],int sz)
{
	for(int i=0;i<sz;i++)
	{
		a[i]=1;
	}
}
void print(int a[],int sz)
{
	for(int i=0;i<sz;i++)
	{
		printf("%d ",a[i]);
		
	}
}

int main()
{
	int a[10]={0};
	int sz=sizeof(a)/sizeof(a[0]);
	Init(a,sz);	
	print(a,sz);
	
	return 0;
}
