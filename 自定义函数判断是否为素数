#include<stdio.h>
int is_prime(int a)
{
	for(int i=2;i<a;i++)
	{
		if(a%i==0)
		return 0;
	}
	return 1;

	
}
int main()
{	
	int a=0;
	printf("请输入一个整数：");
	scanf("%d",&a);
	int b=is_prime(a);
	if(b==1)
	printf("为素数");
	else
	printf("不是素数");
		
	
	return 0;
} 
