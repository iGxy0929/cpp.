#include<stdio.h>
int get_max(int x, int y)
{
	if (x > y)
		return x;
	else
		return y;
}

int main()
{
	int a = 0;
	int b = 0;
	printf("请输入两个数：");
	scanf_s("%d%d", &a, &b);
	int max=get_max(a, b);
	printf("较大值为：%d", max);
	return 0;
}
