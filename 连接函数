#include<stdio.h>
#include<string.h>
int main()
{
	int length1;
	int length2;
	
	char a[20];
	char b[20];
	gets(a);
	gets(b);
	 length1=strlen(a);
	 length2=strlen(b);
	 printf("%d\t%d",length1,length2);	
	 
	 	strcat(a,b);
	 	printf("\n%s",a);
	
	 
	return 0;
}
