#include<stdio.h>
int my_strlen(char *arr)
{
	int count =0;
			while(*arr!='\0')
			{
				count++;
				arr++;
			}
			
	

	return count ;
}
int main()
{
	char arr[1000000];
	gets(arr);
	int len=my_strlen(arr);
	printf("%d",len);
	
	return 0;
}
